1.标定相机内参矩阵，完成
2.计算单应性矩阵H，完成
3.计算旋转矩阵和平移向量，完成
4.找出结构光直线（难点）
5.拟合图像坐标系下的结构光线条方程，在上面任意取出n个点，计算出其在世界坐标系的坐标，怎么算，晚上总结
如果可以拟合出图像坐标系下的结构光的直线方程，则根据单应性矩阵可以算出在世界坐标系下的结构光直线的方程，
也就是靶标平面上的直线方程，这样，在取点的时候，直接在靶标平面上的直线方程上去取点即可，就是世界坐标系的点
6.根据第三步算出的的旋转矩阵和平移向量，计算出光线上的点在相机坐标系的坐标。
7.重复以上步骤，就可以得到足够多的点，可以用来拟合出结构光平面的方程
